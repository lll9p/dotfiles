
# Plugins
# NOTE: `plugin add` is handled by chezmoi run_onchange script
# which runs automatically when nushell version changes.

plugin use inc
plugin use gstat
plugin use formats
plugin use query
plugin use polars

